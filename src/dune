(executable
   (name bap_report_main)
   (public_name bap-toolkit)
   (libraries
        bap_report
        bin_prot
        cmdliner
        str
        parmap
        core_kernel)
   (preprocess (pps ppx_jane)))
